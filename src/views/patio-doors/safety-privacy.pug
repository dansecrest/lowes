extends ../_layouts/patio-doors.pug

append variables
    - section = "Performance"
    - subsection = "Safety & Privacy"

block head
    +head(headData.patioDoors.safetyPrivacy)

prepend container
    +subnav(patioDoorsSections)
    +subnav(patioDoorsSections, true)
    +hero(patioDoorsSafetyPrivacy.hero)

block content
    +inspiration(patioDoorsSafetyPrivacy.inspiration)

prepend footer
    +next(patioDoorsSafetyPrivacy.next)
