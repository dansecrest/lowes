extends ../_layouts/patio-doors.pug

append variables
    - section = "Performance"
    - subsection = "Energy Efficiency"

block head
    +head(headData.patioDoors.energyEfficiency)

prepend container
    +subnav(patioDoorsSections)
    +subnav(patioDoorsSections, true)
    +hero(patioDoorsEnergyEfficiency.hero)

block content
    +inspiration(patioDoorsEnergyEfficiency.inspiration)

prepend footer
    +next(patioDoorsEnergyEfficiency.next)
